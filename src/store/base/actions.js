// import * as http from '../../requests/system'
const actions = {
  // 单据附件数量处理
  // changeBillAttachNumber ({commit}, val) {
  //   http.GetNumber({ID: val})
  //     .then(res => {
  //       let temp = {}
  //       res.map(item => {
  //         temp[item.NewFileType] = item.Num
  //       })
  //       commit('ChangeBillAttachNumber', temp)
  //     })
  // }
}
export default actions
